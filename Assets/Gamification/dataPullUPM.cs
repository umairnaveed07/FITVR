using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class dataPullUPM : MonoBehaviour {
  /*
  private  upmDatabase pullData = new upmDatabase();
  private dataRetrivalUPM data = new dataRetrivalUPM();
  // Start is called before the first frame update
  void Start()
  {
     pullData.dbconnection();
     pullData.getLatestEnteredID();

      data = pullData.getData();
      //int.Parse(data.idGet);
      Debug.Log("Id from UPM is: " + data.idGet);

  }

  // Update is called once per frame
  void Update()
  {

  }*/
}
